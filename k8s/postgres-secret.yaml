apiVersion: v1
kind: Secret
metadata:
  name: modulego-db-secret
  namespace: modulego
type: Opaque
# Values are base64-encoded. Decode with: echo -n 'modulego' | base64
stringData:
  POSTGRES_USER: modulego
  POSTGRES_PASSWORD: modulego
  POSTGRES_DB: modulego
