name: CI

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI placeholder – no checks configured yet."
